@model AnalizaAlgorytmów.Models.ElectionModel

@{
    Layout = null;
}

<!DOCTYPE html> 
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Analiza Algorytmów L1 Kamil Sikorski</title>
    @Scripts.Render("~/bundles/site")
</head>
<body>
    <div>
        @for(int i = 0; i < Model.Data.Count(); i++)
        {
            Model.Data[i].Id = $"convasId{i}";
            @Html.Partial("ElectionGraph", Model.Data[i]);
        }
    </div>
</body>
</html>
